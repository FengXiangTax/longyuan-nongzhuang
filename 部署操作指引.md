# 龙苑农庄网站 · GitHub 部署操作指引

本文档帮你完成「获取 GitHub 钥匙（API Key / Token）→ 创建仓库 → 把钥匙和仓库名交给 AI → AI 一键部署上线」的完整流程。

## 你需要做的 3 件事
1. **生成 GitHub Personal Access Token**（一把「上传钥匙」）
2. **创建一个 GitHub 仓库**
3. **把「仓库名 + Token」发给 AI**

> AI 拿到后会自动执行：初始化 Git → 推送代码 → 开启 GitHub Pages → 给你一个可访问的网站网址。

---

## 第 1 步：获取 GitHub Token（API Key）

GitHub 没有叫「API Key」的东西，对应的是 **Personal Access Token（个人访问令牌）**，简称 **PAT**。它是你授权 AI 上传代码的「钥匙」。

1. 打开 https://github.com 并登录你的账号
2. 点右上角**头像** → **Settings（设置）**
3. 左侧菜单最底部 → **Developer settings（开发者设置）**
4. 点 **Personal access tokens** → **Tokens (classic)**
5. 点 **Generate new token** → **Generate new token (classic)**
6. 填写：
   - **Note（备注）**：填 `longyuan-site-deploy`（方便以后认得）
   - **Expiration（有效期）**：建议选 **7 days 或 30 days**（用完可删，安全）
   - **Select scopes（权限）**：勾选 **`repo`** 这一整组（全选，用于读写仓库）
7. 拉到页面最底部，点 **Generate token（生成令牌）**
8. 生成后，页面会显示一串以 **`ghp_`** 开头的字符串 —— **这是唯一一次显示机会，立刻复制保存！**

> ⚠️ 安全提示：Token 等同于你的账号密码权限，**不要截图发到公开群、不要发给陌生人**。只通过本对话窗口发给 AI 助手即可。

---

## 第 2 步：创建一个 GitHub 仓库

1. 回到 https://github.com，点右上角 **+** → **New repository（新建仓库）**
2. 填写：
   - **Repository name（仓库名）**：建议填 `longyuan-nongzhuang`（或 `longyuan-farm`）
   - **Description（描述）**：填 `龙苑农庄官方网站`
   - 可见性：**Public（公开）**（GitHub Pages 免费版要求公开仓库）
   - **不要**勾选 "Add a README file"（我们已经有 README 了）
3. 点 **Create repository（创建仓库）**
4. 创建成功后，浏览器地址栏会显示类似：
   `https://github.com/<你的用户名>/<仓库名>`
   → 把 **`<你的用户名>/<仓库名>`** 记下来发给 AI

---

## 第 3 步：把「仓库名 + Token」发给 AI

在本对话中回复类似：

> 仓库：`hankzhang/longyuan-nongzhuang`
> Token：`ghp_xxxxxxxxxxxxxxxxxxxx`

AI 收到后会立即执行部署，并回复你网站的访问地址（形如 `https://<用户名>.github.io/<仓库名>/`）。

---

## 第 4 步（由 AI 自动完成，你无需操作）

- `git init` + 首次提交
- 关联你的远程仓库
- `git push` 上传全部代码与图片
- 在仓库 Settings → Pages 开启 GitHub Pages（分支 main，根目录）
- 约 1–2 分钟后，网站即可访问

---

## 常见问题

**Q：Token 泄露了怎么办？**
A：立刻回到 Developer settings → Personal access tokens，把那个 token 删掉，重新生成一个。

**Q：能不能绑定自己的域名 longyuannongzhuang.com？**
A：可以。部署完成后，在仓库 Settings → Pages → Custom domain 填 `www.longyuannongzhuang.com`，并按提示在域名 DNS 添加 CNAME 记录即可。

**Q：部署后 sitemap.xml 里的域名不对？**
A：仓库默认地址是 `<用户名>.github.io/<仓库名>`。若不开自定义域名，可把 `sitemap.xml` 中 `www.longyuannongzhuang.com` 改成实际地址；若已绑定自定义域名则无需改动。
